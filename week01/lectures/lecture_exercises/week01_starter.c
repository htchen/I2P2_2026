#include <limits.h>
#include <stdio.h>

int main(void) {
  int value;
  long long sum = 0;

  /* TODO: read integers until input ends and update sum when appropriate.
     The temporary (void) cast keeps this incomplete starter warning-clean;
     remove it once the implementation uses value. */
  (void)value;

  printf("%lld\n", sum);
  return 0;
}
