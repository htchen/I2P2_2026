#include <limits.h>
#include <stdio.h>

int main(void) {
  int value;
  long long sum = 0;

  /* TODO: read integers until input ends and update sum when appropriate. */
  (void)value;

  printf("%lld\n", sum);
  return 0;
}
